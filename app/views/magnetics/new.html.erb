<% if !@magnetic.errors.empty? %>
  <div class="alert alert-error">
    <ul>
      <% @magnetic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="well">
  <%= form_for @magnetic, html: { multipart: true } do |f| %>
    <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
    </p>
    <p>
    <%= f.label :attachment %>
    <%= f.file_field :attachment %>
    </p>
    
    <%= f.submit "Save", class: "btn btn-primary" %>
  <% end %>
</div>